import "./index.scss";

const Header = () => {
    return (
        <header className="container app-header">
            <p className="fs-4 fw-semibold app-header-brand">Event Planner</p>
        </header>
    );
};

export default Header;
